requests>=2.32.0
# 0.112.0 is the oldest release verified against this code. The floor is not
# cosmetic: suggestions.py and scripts/review_rejections.py pass
# thinking={"type": "adaptive"} and output_config, neither of which the old
# >=0.27.0 floor could satisfy.
anthropic>=0.112.0
